import { SetBodyIdPipe } from './set-body-id.pipe';

describe('SetBodyIdPipe', () => {
  it('create an instance', () => {
    const pipe = new SetBodyIdPipe();
    expect(pipe).toBeTruthy();
  });
});
